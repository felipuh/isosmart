# ISO Smart frontend environment example
#
# Keep the default relative API path when frontend and backend share a reverse proxy.
VITE_API_BASE_URL=/api

# Local development auth bypass must stay disabled unless backend
# ALLOW_LOCAL_AUTH_BYPASS_FOR_TESTS=True and DJANGO_ENV=development.
VITE_LOCAL_AUTH_BYPASS=0

