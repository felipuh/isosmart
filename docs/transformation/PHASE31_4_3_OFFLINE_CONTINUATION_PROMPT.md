# ISO SMART AI — PHASE 31.4.3 OFFLINE CONTINUATION
# CLOSE B1–B4 SUPPORT PRODUCERS AND GOVERNED ADMISSION
# NO POSTGRESQL — NO PHASE 32

Work exclusively in `/home/felipe/proyectos/isosmart`. Before repository
discovery/reading, run `git status --short` and `git diff --check` there and
record exact output. Preserve unrelated work, including the trailing space at
`frontend/src/components/Layout/Sidebar.jsx:28`. Do not reset, stash, clean,
stage, commit, normalize unrelated files or inspect sibling repositories.
Use only `backend/.venv`, Django 4.2.22; never `.venv312`.

Entry: `PHASE 31.4.3 — NOT PROMOTED`, P0=0/P1=4, all B1–B4 open.
The task has not delivered the complete successor architecture. Read:

- `docs/transformation/PHASE31_4_3_SUPPORTING_FIXTURE_AND_GOVERNED_ADMISSION_GATE_REPORT.md`
- `docs/governance/evidence/PHASE31_4_3_SUPPORTING_CONTRACT_AUDIT_V1.json`
- `backend/foundation/test_phase31_4_3_supporting_contract_audit.py`
- `backend/foundation/verify_phase31_4_3_offline.py`

These are diagnostics, not operational authority. Preserve them and every
historical Phase31 report/diagnostic byte-for-byte. Their 103 passing selected
offline tests do not prove complete V2 acceptance. Finish all mandatory semantic
reading using the per-file ledger, including the remaining Phase31.3/31.3.1/
31.4/31.4.1 reports, diagnostic JSONs/tests, ADR-0013–0017, Phase31.3 five-artifact
package, migrations 0009 and 0014–0023, relevant additional support migrations,
complete model/service/evidence/event/audit/security/recovery paths and relevant
Phase28.3 paths. The complete 0022 and Phase26 harness were already read, not
executed. Do not count hashing, parsing or truncated output as complete reading.

Create one complete append-only successor only if all blockers close:

**B4 supporting producer first.** The source census now reaches 37 model
classes, 105 non-null relations and 485 columns. It is not a minimum legal graph
or a lifecycle field taxonomy. Expand SQL-only, nullable conditional, reverse,
event/audit, authority and semantic dependencies to an exact per-row graph.

Use only `opportunity.defer_evaluation` for a NON-OFFICIAL TEST FIXTURE chain:
under_evaluation Opportunity → governed Recommendation/provenance →
AgentDecision → ActionPlan → DryRun → human Approval → ExecutionAuthorization
→ controlled execution/deferred revision/Receipt → Evidence → human
EffectivenessCheck → LearningSignal → ProposalSignal.

Account for published ModelPolicy and AgentDefinition, required published
StandardEdition/KnowledgeLayerRule, exact RequirementControl and Evidence,
AgentRunInput, RecommendationBasis and AgentRunRecommendation. AgentDecision
must precede ActionPlan. Completion requires the output link and exact equality
of input/basis sets. Fully reconcile isolated synthetic catalog creation and
publication with the no-operational-promotion/no-runtime-mutation boundary.
No protected catalog/runtime object changes, selectors, providers or model calls.

Distinguish `governance.curation_audit`, `normative.curation_audit`,
`audit.immutable_audit_log` and dedicated release governance Audit. Enumerate
every actual audit, event and outbox separately; mark native absence explicitly
for catalog operations rather than inventing native events. Preserve all eight
fields of each curation Audit. Immutable Audit identity is generated by SQL
uuidv7: any independently authorized fixture replacement must preserve stream
lock, sequence, prior hash, metadata checks and exact canonical hash semantics.

Design an exact-fixture-only producer for experiment
`a3f8fd64-af24-5b31-977a-bcaf8146c563`. No arbitrary table/model/UUID/actor/
operation/JSON/target inputs. Enumerate every native identity exception and
semantic parity obligation separately. ADR-0017 remains unchanged and limited
to seven Application output IDs. Do not implement or install the producer now.

Owner: NOLOGIN/NOSUPERUSER/NOINHERIT/NOBYPASSRLS/non-table-owner. Executor:
LOGIN/NOSUPERUSER/NOINHERIT/NOBYPASSRLS/non-owner. Specify exact grants and
role-applicable RLS policies per table, fixed pg_catalog search_path, qualified
objects, PUBLIC denial, no dynamic SQL, generic DML or bypass. Grants alone do
not supply RLS access. Existing trusted_tenant_context rejects an outer
transaction: define lawful per-operation composition and immediate precommit
verification instead of nesting existing services blindly.

Resolve the identity wording explicitly: native 0022 derives seven existing
Publication/Activation children using MD5 with version nibble 4, not UUIDv5.
Preserve these native identities; do not alter 0022 or pretend UUIDv5 matches.
The predecessor already defines a separate release_child_id rule. An explicit
successor exception must reconcile the universal UUIDv5 wording before claiming
it satisfied. If that wording remains absolute, report the conflict. Native
Claim and artifact share their UUID across distinct tables; inventory rows by
qualified table plus primary key. Generate UUIDv5 labels for new fixture-assigned
rows and collision-check against all Phase28.3/29/31 identities, distinguishing
references to the same historical row from collisions.

For Effectiveness specify due_at strictly after the deferred revision's
created_at and assessed_at >= due_at, human authority, exact Evidence revisions,
criteria and an actual outcome rule. No predeclared success. Fully resolve
measurement-derived and correction branches. LearningSignal must retain the
eligible leaf/linear history; synthetic=true, automatic_learning=false,
normative=false, production=false belong in its retained semantic envelope,
not invented model columns. Supply all exact nonfresh business text/policies.

**B1 Model B.** Root `bc5f4f17-294d-5abd-b27b-2d296921ffdf` retains eleven exact
non-time values. Define draft bootstrap → separately governed root publication
→ commit → two independent read-only rereads → authentic PostgreSQL canonical
bytes/full-row target hash artifact → Proposal/Delta. Root publication cannot
require its own future Application Receipt. Freeze PostgreSQL18.6 UTF8/UTC,
DateStyle ISO,YMD, IntervalStyle iso_8601, standard_conforming_strings on,
locale provider/LC_COLLATE/LC_CTYPE and extra_float_digits implications. Preserve
0020/0021 canonicalization; never substitute Python timestamp normalization or
predict target timestamps/hash.

**B2/B3.** Preserve compatibility ID `37d3d0bc-4387-581a-a627-f02a011250d1`
and release ID `981e1753-dcd1-5f8d-8b10-49bfaed5eafb`. Supply complete typed
schemas and exact independent read-only producers, observation provenance,
assertions, canonicalization, retention and independent verification. Order:
native Publication → exact PUBLICATION_GRAPH_CHECKPOINT_V1 → compatibility →
release → final Publication closure → independent export/live comparison →
Activation. Checkpoint excludes B2/B3/final closure/self-digest. Remove
postpublication facts from prepublication admission; release never uses
RuntimeAdoption configuration fields. No future numeric digest prebinding.

**Integrated governance.** Adopt execution-ready-v2 only with every field
classified exactly once as PREBOUND_STATIC, EXECUTION_DERIVED_PERSISTED,
EXECUTION_DERIVED_EXTERNAL_AUTHORITY or the seven OUTPUT_ID_MAPPED_BY_ADR0017
positions. Every field needs exact producer/input/canonicalization/freeze/
consumer/retention/verifier, and every created row needs a complete producer
matrix entry. Complete Proposal/Delta, Review (`review_recorded`), actual
business Decision, Authorization, Application, Publication and Activation.

Preserve exact native operation formulas. Separately specify complete
`knowledge-layer-rule-publication-governed-admission/v1` and
`knowledge-layer-rule-activation-governed-admission/v1`: bind exact source,
candidate/provenance, Receipt/parity where applicable, actor, fresh authority,
policy/capability, caller key, native hash, trace, schema/registry, curation
expectations and stage-appropriate evidence/closure. Define concrete admission,
immediate-precommit, retention and replay enforcement. Native replay equality
does not prove governed equality. Original committed authority stays immutable;
fresh authority for replay/read/reconciliation is a distinct attempt. Changed
governed material conflicts; no historical provenance rewrite or invented native
idempotency feature.

Freeze order: 0 static architecture; 1 persisted root target; 1.5 support/signal;
2 Proposal/Delta; 3 Review; 4 Decision; 5 Authorization; 6 Application; 7a native
Publication; 7b checkpoint; 7c compatibility; 7d release; 7e final Publication
closure; 8 Activation; 9 full export/teardown gate. No consumer before producer.
Combine live schema/FK closure and complete semantic registry to fixed point.
Mandatory local material uses RETAIN_FULL_CANONICAL_MATERIAL; fresh external
authority uses EXTERNAL_AUTHORITY_REFERENCE_WITH_FROZEN_PROVENANCE.

Preserve migrations0001–0023, keep0024 absent, preserve authoritative source
and all historical packages. Old B1/B2/B3 numeric expectations are historical
SUPERSEDED_PREEXECUTION_EXPECTATION — NEVER EXECUTED, never execution inputs.
Phase29 remains HISTORICALLY_VERIFIED_NON_REMATERIALIZABLE /
RETENTION_CLOSURE_BREACH; Publication `e97576de-d4ef-520d-8592-d376ed401221`
and missing Audit `12d811ab-c3b4-4615-8972-75008a36e327` are prohibited operational
dependencies. No reconstruction.

Only upon full closure create ADR-0018, successor policy authorizing future
clean Phase31.4 retry eligibility only, complete lifecycle/registry/closureV2,
fixture/admission/evidence contracts, report and meaningful positive/negative
offline acceptance tests. No partial approved-looking package. Validate all
fields/rows/producers/IDs, hashes/preimages, cycles, semantic reachability,
native/governed separation and replay provenance. Run relevant Foundation
tests, in-memory compile, Django check and makemigrations dry-run under guards;
verify protected hashes, new-file whitespace, diff check and final status.

Do not create PostgreSQL/container/roles/database/adapter/fixture producer or
execute migrations, lifecycle, resolver, harnesses, network or real AdminApps.
Operational tests NOT EXECUTED; all DB/lifecycle/runtime/production/normative/
learning/external/Phase29-reconstruction effects and attempt counters zero.

Promote only with mandatory_reading_complete=true, B1–B4 closed, all required
V2 artifacts complete, unknown_binding_count=0, fixed_hash_without_preimage=0,
P0=0/P1=0 and preserved boundaries. Otherwise emit
`PHASE 31.4.3 — NOT PROMOTED`, exact residual blockers and one offline
continuation. Only if promoted supply one self-contained Phase31.4 CLEAN RETRY2
prompt consuming V2, with ADR0017 parity, Publication closure before Activation,
export/live comparison and governed teardown/post-teardown verification. No
RuntimeAdoption and no Phase32.
